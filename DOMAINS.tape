# DOMAINS.tape — airgenome domain roster
# Roster rows (`@domain NAME := "path"`) are AUTHORITATIVE for the /domain CLI.
@V := "domains" :: index

# --- roster (nodes) ---
@domain C-PORT := "./domains/C-PORT.md"
