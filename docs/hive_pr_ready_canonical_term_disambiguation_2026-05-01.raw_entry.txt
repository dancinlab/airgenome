raw 258 new "canonical-tool-term-disambiguation-mandate - every reserved tool term in the hexa-lang governed cluster (kick / harvest / label / forecast / drill / blowup / revive / ingest / absorb / kick-tree / omega-ingest / smash / free / meta-closure / absolute — 15 entries as of 2026-05-01) MUST resolve to its canonical tool first when received by an AI agent, citing the canonical impl path before any alternative interpretation. Disambiguation gate has finite 3-vocab semantics: (1) `canonical` — term has canonical mapping AND no explicit user override AND context does NOT exclude tool invocation → use canonical impl; (2) `natural_language` — context obviously excludes tool invocation (e.g. 'kick the ball', 'absolute path', C `free()` literal usage) → permit natural-language; (3) `explicit_override` — user explicitly states 'no, I mean X' / `--no-canonical` / 'the verb' → permit alternative, still cite canonical for trace audit. Agent MUST emit per-decision trailer `[disambiguation: <canonical|natural_language|explicit_override> term=<X> canonical_path=<P>]`. Canonical mapping table SSOT lives at airgenome/docs/raw_canonical_tool_term_canonical_mapping_2026-05-01.jsonl (15 rows × 12 fields per row: term + canonical_tool + canonical_path + canonical_invocation + canonical_alias + semantic + ambiguity_examples ≥4 + established_date + established_by + cross_repo_evidence ≥1 + falsifier + version). Origin (raw 33 verbatim Korean carve-out): user mandate 2026-05-01 'kick 하라고 하면 nexus cli kick 사용이라는것을 인식을 잘못하는데 해당 raw 강화 필요할듯, ai-native 고려, ai agent 고려 만점 기준 강화고갈시까지 강화'. Sourced from airgenome 2026-05-01 cycle: 30d baseline transcript-walk audit (5662 ~/.claude/projects/*.jsonl files; 1307 with term hits; 4744 total user-mentions of 15 canonical terms) measured 97.34% overall fail_rate (4618 alternative / 4744 total) — kick 93.59% (730/780), drill 92.63% (905/977), blowup 99.45% (539/542), label 100% (458/458), free 100% (767/767), absolute 100% (452/452), absorb 100% (203/203), ingest 100% (192/192), smash 100% (195/195), harvest 98.81% (83/84), forecast 100% (49/49), meta-closure 100% (28/28), revive 100% (13/13), omega-ingest 100% (4/4) — 14 of 15 terms exceed F-RAW258-1 5% threshold ≫ justified_evidence strengthening class (raw 102 ADD-new gate). Banned: agent receives 'kick X' and dispatches Bash kill -9 / direct hexa engine call / ad-hoc bisection script instead of `nexus kick <topic>` (raw 99 / raw 100 violation). Banned: agent receives 'harvest X' and writes ad-hoc scraper / generic ETL ingest tool instead of `anima harvest <stream>`. Banned: agent receives 'label site-X' or 'forecast site-X' and writes generic labeling/forecasting code instead of own 7 / own 6 site-list filter under airgenome/modules/filters/data/. Banned: agent receives 'drill seed=...' / 'blowup' / 'smash' / 'free' / 'meta-closure' / 'absolute' and runs Bash/realpath/free(1)/Python lambda-closure-example instead of canonical `nexus <subcommand> --seed`. Banned: agent receives 'revive ledger-id' and runs git reflog cherry-pick instead of `hive convergence revive <id>`. Banned: agent receives 'absorb X' and writes manual ledger-row append instead of automatic raw 135 discovery-auto-absorption pipeline. Banned: agent receives 'kick-tree' and runs `tree design/kick/` instead of `nexus kick tree`. Banned: agent receives 'omega-ingest' and writes ad-hoc ordinal arithmetic instead of `anima harvest <stream> --mode omega`. Allowed: natural_language interpretation when context obviously excludes tool invocation (`kick the ball`, `absolute path` referring to filesystem path-resolution literally, C `free()` used in a memory-management discussion, `harvest the wheat` in agriculture context, `<label>` HTML element discussion, `position: absolute` CSS, `Smash Bros` game mention, `feel free` idiom) — disambiguation gate vocab=`natural_language`, NO canonical-tool dispatch required. Allowed: explicit_override (`I want the literal English meaning of <term>`, `--no-canonical`, `not the tool`) — gate vocab=`explicit_override`, agent records canonical for trace but obeys override. Allowed: external-domain term occurrence in non-cluster files (physics simulation light-absorb coefficient under physics/, weather forecast UI under unrelated repo) — pre-empts cluster canonical when domain context is unambiguous. ENFORCEMENT TRIAD per raw 95: (a) tool/canonical_term_disambiguation_lint.hexa scans agent traces (state/agent_self_replay_audit/audit.jsonl + ~/.claude/projects/*.jsonl 30d window) for term-occurrence vs canonical-path-cited within N=5 next assistant tool_use blocks (advisory-tier audit row at registration; promotion to hard-block after 30d zero-violation streak — raw 256 calibration pattern); (b) raw 197 agent self-replay during turn-emission rejects banned alt-dispatch patterns (Bash kill on kick / generic ETL on harvest / git reflog on revive / etc) with 1-line `__DISAMBIGUATION__ FAIL term=<X> alt=<class> expected=<canonical_path>` trailer; (c) session-start brief-frame load (raw 30 / raw 236) surfaces raw 258 + canonical mapping JSONL as canonical lookup table for new agent sessions — agent recognition discipline so handoff doesn't re-derive table each cycle (raw 257 same pattern for closure-ETA). Five cognitive frameworks (raw 117 5-check ≥3): (i) lexical-semantics field theory (Lyons 1977 / Cruse 2000 — sense-network dominance in domain-specific clusters), (ii) Stroop-effect interference resolution (deterministic priority arbiter when word triggers two responses), (iii) Apple HIG / GNU coding-style command-name discipline (reserved-name canonical-first), (iv) DNS-style authoritative resolution (RFC 1034 — canonical mapping = cluster zone file with recursive-resolver fallback), (v) Schelling-point coordination (Schelling 1960 — table = focal point preventing per-agent re-derivation). Five realization channels (raw 117 5-check ≥3): (i) machine-readable mapping table JSONL airgenome/docs/raw_canonical_tool_term_canonical_mapping_2026-05-01.jsonl, (ii) AI-agent system prompt injection at session start (raw 30 brief-frame), (iii) hive raw entry (this raw 258) with tri-layer enforce per raw 94/95, (iv) paired lint tool/canonical_term_disambiguation_lint.hexa (audit-tier at registration, hard-block after 30d streak), (v) raw 135 discovery-auto-absorption registry emission per disambiguation decision. Three counter-examples (raw 117 5-check ≥3): (i) natural-language carry-over in code comment / docstring / chat ('kick the ball' literary), (ii) external-domain term in non-cluster file (physics absorption coefficient / weather forecast / shipping label), (iii) explicit user override ('I want the literal English meaning of free here, not the nexus cli'). Falsifier F-RAW258-1: 30d post-registration any term shows ≥5% fail_rate via canonical_term_disambiguation_lint.hexa transcript-walk audit re-run (current baseline 97.34% overall — convergence target ≤5% within 30d post-registration). F-RAW258-2: false-positive rate (canonical forced where natural-language correct) >2% of disambiguation decisions in 30d → relax disambiguation gate, add context-exclusion rules, retire OR strengthen. F-RAW258-3: any canonical_path in mapping JSONL 404s for >7 days → mapping retired pending update, raw 91 honest C3 row emitted. F-RAW258-4: mapping count exceeds 50 entries within 1y → trigger genus consolidation review per raw 106; potentially split into nexus-canonical / anima-canonical / airgenome-canonical sub-rules. F-RAW258-5: any new term added without ≥1 cross_repo_evidence row → reject addition; cite raw 240 5-check inflation gate. classifier-version raw_240_v2.6_800_800. raw 91 honest C3 ≥5: (1) tool/canonical_term_disambiguation_lint.hexa advisory-tier (warn-only) at registration — promotion to hard-block needs 30d zero-violation streak (raw 256 / raw 257 calibration pattern); (2) baseline measurement script classifies tool_use canonical match via regex pattern list — false-negatives possible when canonical invocation appears in different argv shape than canonical_patterns list (e.g. `nexus kick` invoked via Bash subprocess with quoted argv would still register as Bash alternative if regex doesn't see `nexus kick` token) — refinement via lint v1.1 expected; (3) `free` term has 25 natural_language flagged (3.3% of 767 mentions — primarily C `free()` memory-management discussions and `feel free` idiom) but classification stays `alternative` because no canonical tool was invoked — gate vocab=`natural_language` correctly applied at agent-decision time would suppress this; lint v1.1 NL-pre-filter expected; (4) 30d transcript walk covers 5662 files but ~/.claude/projects/ is single-host (Mac local) — cross-host (ubu1/ubu2/hetzner) transcript coverage gap; remote bg-agent transcripts NOT in window — follow-up cycle needed; (5) 5 of 15 terms have low sample size (kick-tree 0, omega-ingest 4, revive 13, meta-closure 28, forecast 49) — strengthening_class `preventive` for kick-tree only; remaining 14 terms have justified_evidence; (6) AI-agent system prompt injection mechanism (channel ii) is brief-frame load (raw 30 / raw 236) — depends on Claude Code session bootstrap surface; (7) explicit_override grammar is loose (`no, I mean X` pattern) — future v1.1 may formalize via `--no-canonical` flag in CLI surface. enforce tool/canonical_term_disambiguation_lint.hexa. enforce-layer hive-agent + cli-lint + airgenome-airdrop. proof airgenome/docs/raw_canonical_tool_term_disambiguation_proposal.md (V2.6 800/800 derivation §8) + airgenome/docs/raw_canonical_tool_term_disambiguation_proposal.rubric.jsonl (one row per block + 15 mapping sub-rows, machine-grep magic CTTD-v1) + airgenome/docs/raw_canonical_tool_term_canonical_mapping_2026-05-01.jsonl (15 entries × 12 fields) + airgenome/state/canonical_term_baseline_audit/baseline_30d_2026-05-01.jsonl (5662 transcripts walked, 4744 mentions, 97.34% baseline fail_rate). spec-form forward-spec. paired-roadmap-id P2026-05-01.258. deps raw:nexus-kick-canonical-cli-mandate. deps raw:alternative-dispatcher-clause. deps raw:strengthen-existing-mandate. deps raw:discovery-auto-absorption-mandate. deps raw:agent-self-replay-mandate. deps raw:brief-frame-session-start-mandate. deps raw:honest-c3-disclosure-mandate. deps raw:genus-naming-mandate. deps raw:cross-repo-design-strategy-mandate. category meta-triad. applies-to ai-agent-turn-emission + cli-lint-audit + session-start-brief-frame. phase decision-time + post-decision-trailer + 30d-window-audit. severity warn. Distinct from raw 99 (nexus-kick-canonical-CLI single-entry runtime dispatch scope — raw 258 = AI-agent term-recognition lookup-table SPECIALIZATION across 15-term mapping). Distinct from raw 100 (alternative-dispatcher Mac fallback escape hatch — raw 258 = canonical-first recognition layer prior to fallback). Distinct from raw 102 (STRENGTHEN-existing meta-directive — raw 258 = the strengthening it directs). Distinct from raw 6 r50 (cycle-descriptor-naming — raw 258 = generic reserved-tool-term disambiguation across cluster). Distinct from raw 256 (path-env-abstraction sister-repo path-resolution — raw 258 = tool-invocation term-resolution). raw 117 5-check: genus slug ✓ (canonical-tool-term-disambiguation-mandate, 4-genus composite canonical+tool-term+disambiguation+mandate per raw 106) + ≥3 cognitive frameworks ✓ (5 declared: lexical-semantics + Stroop + HIG/GNU + DNS-RFC1034 + Schelling) + ≥3 realization channels ✓ (5 declared: machine-readable JSONL + brief-frame + raw entry + paired lint + raw 135 absorption) + ≥3 counter-examples ✓ (3 declared: natural_language + external-domain + explicit_override) + ≥5 falsifiers ✓ (F-RAW258-1..5). Cross-repo per raw 47: nexus (kick/drill/smash/free/meta-closure/absolute/kick-tree — 7 terms), anima (harvest/ingest/omega-ingest — 3 terms), airgenome (label/forecast — 2 terms), hive (revive/absorb — 2 terms), hexa-lang (downstream consumer of all canonical CLI surfaces — inherits all 15 per raw 0 root-ssot consumer), n6-architecture (atlas health surface kick witness landing — inherits via raw 47). 30d ramp post-registration. proof-obligations tool/canonical_term_disambiguation_lint.hexa-selftest-M5-PASS-at-cycle+1 + raw-258-self-apply-PASS-at-registration + 30d-baseline-fail-rate-converge-≤5pct-at-cycle+30. omega-stop fixpoint-convergence. Genus slug per raw 106. Cross-repo per raw 47."
